======================
Mozaik HR Work Address
======================

Allow to configure the work address of employees on the department level

Usage
=====

Configure the work address on a department if the address is the same for all children departments and all employees belonging to these departments.
NB: The work address on such employees will be filled by default with the address of the department (and changed if the department address changes), but this can be changed individually on the employee.
