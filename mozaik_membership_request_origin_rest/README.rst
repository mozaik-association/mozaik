=====================================
Mozaik Membership Request Origin Rest
=====================================

Add origin to membership request rest services.

Purpose
=======

Glue module between mozaik_membership_rest and mozaik_membership_request_origin.
Allows to get the list of origins via API and set an origin when creating a
membership request from the API.
